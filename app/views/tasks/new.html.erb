<% content_for :title, "Ny uppgift" %>

<%= form_for @task do |f| %>
    <%= render 'shared/error_messages', obj: f.object %>

    <%= render 'task_fields', f: f %> <!-- rendame f maybe !-->
    <%= render 'user_fields', f: f %>

    <%= f.submit "Skapa uppgift", class: "btn btn-primary", data: { disable_with: "Skapar uppgift.." } %>
    <%= link_to "Avbryt", tasks_path, class: "btn btn-primary" %>
<% end %>
